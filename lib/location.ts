/** Service area: update wording in one place. No state name by request. */
export const AREA_LABEL = "Brighton Central";

/** Small caps / labels: use where a short area name is still needed. */
export const AREA_LABEL_SHORT = "Brighton Central";
